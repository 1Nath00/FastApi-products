{
    "providers": [
        "python"
    ],
    "install": {
        "cmd": "pip install -r requirements.txt"
    },
    "build": {
        "cmd": "echo 'Building...'"
    },
    "start": {
        "cmd": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
}